# Generated by `npm run build` (scripts/build-wasm.sh) via the wasm-bindgen
# toolchain. Rebuilt by the `prepack` hook before `npm pack`/`npm publish`, so
# the published tarball carries these files (see the `files` field) while the
# repository does not track generated binaries.
wasm/
node_modules/
