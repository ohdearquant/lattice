# Build
/target
debug

# Cargo
**/*.rs.bk
Cargo.lock

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# MSVC
*.pdb

# Mutation testing
**/mutants.out*/

# Models (large files, don't commit)
*.safetensors
*.bin
*.onnx
*.gguf
# ...except the two small committed HF differential golden tensors (ADR-069, ~1 MB total).
# Exact paths only (not a glob) so an arbitrary future .bin dropped in this directory stays ignored.
!tests/fixtures/vision/vit_pre_merger_f32.bin
!tests/fixtures/vision/vit_post_merger_f32.bin

# Review scratch artifacts (repo-root only)
/review*.md
/pr_review*.md

*__pycache__/
uv.lock

# Internal workspace artifacts (audits, notes, local tooling) — local-only
.khive/
